import { Container, Typography } from "@mui/material";

export const FormRetorno = () => {

    return (
        <Container >
            <Typography>
                Formulario enviado com sucesso!
            </Typography>
        </Container>
    );
};