import { Box, Container, Typography } from "@mui/material";

export const FormConfirmacao = () => {

    return (
        <Container >
            <Typography>
                Formulario enviado com sucesso!
            </Typography>
        </Container>
    );
};
